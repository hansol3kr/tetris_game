# .NET
bin/
obj/
*.user

# Godot 4
.godot/
game/build/
export_presets.cfg.bak

# 빌드 산출물 / 재생성 가능한 것들 (GitHub·Codemagic에 올릴 필요 없음)
dist/
# 안드로이드 빌드 템플릿: 에디터의 "Install Android Build Template"로 재설치
game/android/

# OS / editor
.DS_Store
Thumbs.db
.vs/
.idea/
*.swp

# Godot .NET generated
game/.mono/
mono_crash.*.json

# Node matchmaking server
server/node_modules/
npm-debug.log*

# iOS App Store Connect API 키 + 기타 비밀 — 절대 커밋 금지 (.example 템플릿/README 만 커밋).
ios/appstore_connect.env
# private_keys/ 안의 모든 파일 무시 (.p8, git_key 등) — README 만 예외로 추적.
ios/private_keys/*
!ios/private_keys/README.md
